import React from 'react';
import styled from '@emotion/styled';

type PropsType = {

}

const Maps = (props: PropsType): JSX.Element => {
    return (
        <div>

        </div>
    );
}

export default Maps;